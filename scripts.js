let state = 'idle'
let user = null
let calculated = 1

// Only allowed to change below


function logCalc() {
    const isString = calculated;
    const calculatedAsNumber = calculated;
    const calculated = calculated + 1


}







const calcUser = () => {

    if (calculated = 1) { user = 'John' }
    if (calculated = 1) { state = 'requesting' }
    if (calculated = 1) { state = 'idle' }
}

const checkUser = () => {

    if (user === 'John' && state === 'requesting') {

        console.log(`User: ${user} (${calculated})`)
    }
}
console.log(`user: ${user} (${calculated + 1})`)


// Only allowed to change code above

checkUser()
calcUser()

checkUser()
calcUser()

checkUser()
calcUser()

checkUser()
calcUser()

checkUser()
calcUser()


